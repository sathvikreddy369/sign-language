# Server
PORT=5000
JWT_SECRET=change-me

# Predictor service (where ML inference runs)
# If you still run the existing Python Flask predictor, set:
# PREDICTOR_URL=http://localhost:5001
PREDICTOR_URL=http://localhost:5001

# SQLite DB file path (created automatically)
DATABASE_FILE=./data/asl.db
